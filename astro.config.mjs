// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // El bloque 'vite' que contenía el plugin de tailwind ha sido eliminado.
});